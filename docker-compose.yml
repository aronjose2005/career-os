services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}

  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - api
