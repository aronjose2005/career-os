server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / { try_files $uri /index.html; }
  location /api/ {            # same-origin proxy → no browser CORS headaches
    proxy_pass http://api:3000/api/;
    proxy_set_header Host $host;
  }
}
